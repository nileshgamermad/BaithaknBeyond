{
  "name": "baithakn-beyond",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Static site deployment for Baithak & Beyond using GitHub Pages.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/post-build.mjs",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.29.1",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.3.1",
    "wrangler": "^4.75.0"
  }
}
